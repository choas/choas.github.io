---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ site.url }}/feed.xml" rel="self" type="application/atom+xml" />
  <link href="{{ site.url }}/" rel="alternate" type="text/html" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/feed.xml</id>
  <title type="html">{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  {% for post in site.posts limit:20 %}
  <entry>
    <title type="html">{{ post.title | xml_escape }}</title>
    <link href="{{ site.url }}{{ post.url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>
    <id>{{ site.url }}{{ post.id }}</id>
    {% if post.image %}
    <media:content url="{{ site.url }}/images/{{ post.image }}" medium="image" />
    <media:thumbnail url="{{ site.url }}/images/{{ post.image }}" />
    {% endif %}
    {% if post.excerpt %}
    <summary type="html"><![CDATA[{{ post.excerpt | strip_html | truncate: 300 }}]]></summary>
    {% endif %}
    <content type="html"><![CDATA[{% if post.image %}<img src="{{ site.url }}/images/{{ post.image }}" alt="{{ post.title }}" />{% endif %}{{ post.content }}]]></content>
    {% if post.author %}
    <author>
      <name>{{ post.author | xml_escape }}</name>
    </author>
    {% endif %}
    {% if post.tags %}
    {% for tag in post.tags %}
    <category term="{{ tag | xml_escape }}" />
    {% endfor %}
    {% endif %}
  </entry>
  {% endfor %}
</feed>
